'use strict';

// var processInclude = require('./util');

// $(document).ready(function () {
//     processInclude(require('./product/detail'));
// });


$(document).ready(function(event) {

    $('.buy-as-gift').on('click',function(){
        
        //var pid = $('.product-detail.product-wrapper').attr('data-pid'); // get id of current product
        //alert( "Current product id: " + pid );
        // code to save checkbox value in productLineItem custom attribute
    });      
     
 });